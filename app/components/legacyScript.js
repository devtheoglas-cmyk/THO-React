// Verbatim contents of the original script.js, exported as a string and run on mount.
const legacyScript = "const menuButton = document.querySelector(\"[data-menu]\");\nconst menuPanel = document.querySelector(\"[data-menu-panel]\");\nconst figmaPage = document.querySelector(\".figma-page\");\nconst figmaFrame = document.querySelector(\".figma-frame\");\nconst hoverZoomHotspots = document.querySelectorAll(\n  \".hotspot:not(.home-product-card):not(.footer-link):not([class*='product-card-'])\",\n);\nconst scrollTextReveals = document.querySelectorAll(\"[data-scroll-text-reveal]\");\nconst productTiltLayers = document.querySelectorAll(\"[data-product-tilt]\");\n\nconst updateFigmaImageCrops = () => {\n  if (!figmaPage || !figmaFrame) {\n    return;\n  }\n\n  const pageRect = figmaPage.getBoundingClientRect();\n  const imageUrl = new URL(figmaFrame.getAttribute(\"src\"), window.location.href).href;\n\n  hoverZoomHotspots.forEach((hotspot) => {\n    const hotspotRect = hotspot.getBoundingClientRect();\n    const x = hotspotRect.left - pageRect.left;\n    const y = hotspotRect.top - pageRect.top;\n\n    hotspot.dataset.hoverZoom = \"true\";\n    hotspot.style.setProperty(\"--hover-bg-image\", `url(\"${imageUrl}\")`);\n    hotspot.style.setProperty(\"--hover-bg-size\", `${pageRect.width}px auto`);\n    hotspot.style.setProperty(\"--hover-bg-x\", `${-x}px`);\n    hotspot.style.setProperty(\"--hover-bg-y\", `${-y}px`);\n  });\n\n  scrollTextReveals.forEach((reveal) => {\n    const copy = reveal.querySelector(\".scroll-text-reveal-copy\");\n\n    if (!copy) {\n      return;\n    }\n\n    const revealRect = reveal.getBoundingClientRect();\n    const x = revealRect.left - pageRect.left;\n    const y = revealRect.top - pageRect.top;\n\n    copy.style.setProperty(\"--figma-crop-image\", `url(\"${imageUrl}\")`);\n    copy.style.setProperty(\"--figma-crop-size\", `${pageRect.width}px auto`);\n    copy.style.setProperty(\"--figma-crop-x\", `${-x}px`);\n    copy.style.setProperty(\"--figma-crop-y\", `${-y}px`);\n  });\n};\n\nif (figmaFrame) {\n  if (figmaFrame.complete) {\n    updateFigmaImageCrops();\n  } else {\n    figmaFrame.addEventListener(\"load\", updateFigmaImageCrops, { once: true });\n  }\n\n  window.addEventListener(\"resize\", updateFigmaImageCrops);\n}\n\nproductTiltLayers.forEach((layer) => {\n  const resetTilt = () => {\n    layer.classList.remove(\"is-tilting\");\n    layer.style.setProperty(\"--tilt-rotate-x\", \"0deg\");\n    layer.style.setProperty(\"--tilt-rotate-y\", \"0deg\");\n    layer.style.setProperty(\"--tilt-scale\", \"1\");\n  };\n\n  layer.addEventListener(\"pointermove\", (event) => {\n    if (event.pointerType === \"touch\") {\n      return;\n    }\n\n    const rect = layer.getBoundingClientRect();\n    const x = (event.clientX - rect.left) / rect.width - 0.5;\n    const y = (event.clientY - rect.top) / rect.height - 0.5;\n    const maxTilt = 7;\n\n    layer.classList.add(\"is-tilting\");\n    layer.style.setProperty(\"--tilt-rotate-x\", `${(-y * maxTilt).toFixed(2)}deg`);\n    layer.style.setProperty(\"--tilt-rotate-y\", `${(x * maxTilt).toFixed(2)}deg`);\n    layer.style.setProperty(\"--tilt-scale\", \"1.015\");\n  });\n\n  layer.addEventListener(\"pointerleave\", resetTilt);\n  layer.addEventListener(\"pointercancel\", resetTilt);\n});\n\nif (menuButton && menuPanel) {\n  let menuCloseTimer = null;\n\n  // Drop the panel directly below the MENU trigger, left-aligned to it.\n  const positionMenu = () => {\n    const r = menuButton.getBoundingClientRect();\n    menuPanel.style.top = `${Math.round(r.bottom + 10)}px`;\n    menuPanel.style.left = `${Math.round(r.left)}px`;\n  };\n\n  const openMenu = () => {\n    window.clearTimeout(menuCloseTimer);\n    positionMenu();\n    menuPanel.classList.add(\"is-open\");\n  };\n\n  const closeMenu = () => {\n    menuPanel.classList.remove(\"is-open\");\n  };\n\n  // Close shortly after the pointer leaves both the trigger and the panel,\n  // so moving from the button onto the panel keeps it open.\n  const scheduleClose = () => {\n    window.clearTimeout(menuCloseTimer);\n    menuCloseTimer = window.setTimeout(closeMenu, 220);\n  };\n\n  // Hover to open — ONLY on real-hover (desktop) pointers. On touch, mouseenter\n  // fires on tap and would open-then-click-close, so touch relies on click alone.\n  if (window.matchMedia(\"(hover: hover) and (pointer: fine)\").matches) {\n    menuButton.addEventListener(\"mouseenter\", openMenu);\n    menuButton.addEventListener(\"mouseleave\", scheduleClose);\n    menuPanel.addEventListener(\"mouseenter\", openMenu);\n    menuPanel.addEventListener(\"mouseleave\", scheduleClose);\n  }\n\n  // Click toggles (touch devices / keyboard)\n  menuButton.addEventListener(\"click\", (event) => {\n    event.preventDefault();\n    if (menuPanel.classList.contains(\"is-open\")) { closeMenu(); }\n    else { openMenu(); }\n  });\n\n  menuPanel.addEventListener(\"click\", (event) => {\n    if (event.target instanceof HTMLAnchorElement) {\n      closeMenu();\n    }\n  });\n\n  window.addEventListener(\"resize\", () => {\n    if (menuPanel.classList.contains(\"is-open\")) {\n      positionMenu();\n    }\n  });\n}\n\ndocument.addEventListener(\"keydown\", (event) => {\n  if (event.key === \"Escape\" && menuPanel) {\n    menuPanel.classList.remove(\"is-open\");\n  }\n});\n\ndocument.querySelectorAll(\"[data-filter]\").forEach((button) => {\n  button.addEventListener(\"click\", () => {\n    document.body.dataset.productFilter = button.dataset.filter;\n  });\n});\n\n// \"Return\"/back links: go to the actual previous page in this tab (calendar, home,\n// products, wherever the user came from) instead of the hard-coded href. Falls back\n// to the href for direct visits / cross-site entries.\ndocument.querySelectorAll(\"[data-back]\").forEach((el) => {\n  el.addEventListener(\"click\", (e) => {\n    try {\n      const sameOrigin = document.referrer && new URL(document.referrer).origin === location.origin;\n      if (window.history.length > 1 && sameOrigin) { e.preventDefault(); window.history.back(); }\n    } catch (err) {}\n  });\n});\n\ndocument.querySelectorAll(\"[data-calendar-view-more]\").forEach((button) => {\n  button.addEventListener(\"click\", () => {\n    document.body.dataset.calendarExpanded = \"true\";\n  });\n});\n\ndocument.querySelectorAll(\"[data-contact-submit]\").forEach((button) => {\n  button.addEventListener(\"click\", () => {\n    document.body.dataset.contactSubmitted = \"true\";\n  });\n});\n\nconst scrollAnimatedElements = document.querySelectorAll(\"[data-animate-on-scroll]\");\n\nif (scrollAnimatedElements.length > 0) {\n  if (\"IntersectionObserver\" in window) {\n    const animationObserver = new IntersectionObserver(\n      (entries) => {\n        entries.forEach((entry) => {\n          if (entry.isIntersecting) {\n            // Play once, then stop watching so the animation never replays.\n            entry.target.classList.add(\"is-visible\");\n            animationObserver.unobserve(entry.target);\n          }\n        });\n      },\n      { threshold: 0.35 }\n    );\n\n    scrollAnimatedElements.forEach((element) => {\n      animationObserver.observe(element);\n    });\n  } else {\n    scrollAnimatedElements.forEach((element) => {\n      element.classList.add(\"is-visible\");\n    });\n  }\n}\n\n// About page: draw the heart-shaped line around the hand on first view. The\n// reveal is a mask stroke whose dash offset animates from \"fully hidden\"\n// (offset = path length) to \"fully drawn\" (offset = 0).\nconst heartStrokes = document.querySelectorAll(\".heart-reveal-stroke\");\nheartStrokes.forEach((heartStroke) => {\n  if (typeof heartStroke.getTotalLength !== \"function\") return;\n  const heartSvg = heartStroke.closest(\".heart-draw\");\n  const length = heartStroke.getTotalLength();\n  heartStroke.style.strokeDasharray = String(length);\n  heartStroke.style.strokeDashoffset = String(length);\n\n  const drawHeart = () => {\n    heartStroke.style.strokeDashoffset = \"0\";\n  };\n\n  const prefersReducedMotion =\n    window.matchMedia && window.matchMedia(\"(prefers-reduced-motion: reduce)\").matches;\n\n  if (prefersReducedMotion) {\n    drawHeart();\n  } else if (heartSvg && \"IntersectionObserver\" in window) {\n    const heartObserver = new IntersectionObserver(\n      (entries) => {\n        entries.forEach((entry) => {\n          if (entry.isIntersecting) {\n            drawHeart();\n            heartObserver.unobserve(entry.target);\n          }\n        });\n      },\n      { threshold: 0.4 }\n    );\n    heartObserver.observe(heartSvg);\n  } else {\n    drawHeart();\n  }\n});\n\n// Standards section: pin it and step through three blocks as the user scrolls\n// (like the reference video). The panel stays fixed; the heading + copy slide\n// up and fade as the matching diagram cross-fades in the centre\n// (Cultivation -> Direct Sourcing -> Regrowth Cycle).\nconst standardsSection = document.querySelector(\".home-standards-section\");\nif (standardsSection) {\n  const steps = Array.from(standardsSection.querySelectorAll(\".standards-step\"));\n  const diagrams = Array.from(\n    standardsSection.querySelectorAll(\".standards-diagram\")\n  );\n  const dots = Array.from(\n    standardsSection.querySelectorAll(\".standards-progress span\")\n  );\n  const prefersReducedMotion = window.matchMedia(\n    \"(prefers-reduced-motion: reduce)\"\n  ).matches;\n\n  if (steps.length > 1 && !prefersReducedMotion) {\n    const N = steps.length;\n    const TRACK_VH = 1.15 * N; // total scroll length, in screens\n    const SLIDE = 70; // vertical travel of the text, in px\n    const FADE_SPAN = 0.62; // how quickly a step fades with distance\n    const smooth = (t) => t * t * (3 - 2 * t);\n    let ticking = false;\n\n    const layout = () => {\n      standardsSection.style.height = window.innerHeight * TRACK_VH + \"px\";\n    };\n\n    // Map scroll progress (0..1) to a step position (0..N-1) with a hold on\n    // each step and an eased transition between them.\n    const progressToPosition = (p) => {\n      const segs = N + (N - 1); // holds + transitions\n      const unit = 1 / segs;\n      const idx = Math.min(segs - 1, Math.floor(p / unit));\n      const local = (p - idx * unit) / unit;\n      if (idx % 2 === 0) return idx / 2; // hold on a step\n      return (idx - 1) / 2 + smooth(local); // transition to the next step\n    };\n\n    const update = () => {\n      ticking = false;\n      const vh = window.innerHeight;\n      const distance = standardsSection.offsetHeight - vh;\n      if (distance <= 0) return;\n\n      const p = Math.min(\n        1,\n        Math.max(0, -standardsSection.getBoundingClientRect().top / distance)\n      );\n      const pos = progressToPosition(p);\n      const active = Math.round(pos);\n\n      steps.forEach((step, i) => {\n        const d = i - pos; // <0 above, >0 below\n        const opacity = Math.max(0, 1 - Math.abs(d) / FADE_SPAN);\n        step.style.opacity = opacity.toFixed(3);\n        step.style.transform = `translate3d(0, ${(d * SLIDE).toFixed(1)}px, 0)`;\n      });\n\n      diagrams.forEach((diagram, i) => {\n        const d = i - pos;\n        const opacity = Math.max(0, 1 - Math.abs(d) / FADE_SPAN);\n        diagram.style.opacity = opacity.toFixed(3);\n        diagram.style.transform = `scale(${(0.94 + 0.06 * opacity).toFixed(3)})`;\n      });\n\n      dots.forEach((dot, i) => dot.classList.toggle(\"is-active\", i === active));\n    };\n\n    const onScroll = () => {\n      if (!ticking) {\n        ticking = true;\n        requestAnimationFrame(update);\n      }\n    };\n\n    const onResize = () => {\n      layout();\n      update();\n    };\n\n    standardsSection.classList.add(\"standards-pinned\");\n    layout();\n    update();\n\n    window.addEventListener(\"scroll\", onScroll, { passive: true });\n    window.addEventListener(\"resize\", onResize);\n  }\n}\n\n// What We Do: the four process steps cross over inside one pinned, full-page\n// stage with no empty gap — the current step fades out while the next slides up\n// into place. Each panel is sized (in CSS) to fit fully, so nothing is cut off.\nconst wwdStack = document.querySelector(\".wwd-stack\");\nconst wwdPanels = wwdStack\n  ? Array.from(wwdStack.querySelectorAll(\".wwd-panel\"))\n  : [];\nif (\n  wwdStack &&\n  wwdPanels.length > 1 &&\n  window.matchMedia(\"(min-width: 761px)\").matches &&\n  !(window.matchMedia && window.matchMedia(\"(prefers-reduced-motion: reduce)\").matches)\n) {\n  const N = wwdPanels.length;\n  const FADE_SPAN = 0.92; // cross-fade width, in step units\n  const RISE_IN = 180; // px the next step travels up from below as it steps in\n  const RISE_OUT = 30; // px the leaving step drifts up as it fades out\n  const smooth = (t) => t * t * (3 - 2 * t);\n  let ticking = false;\n\n  wwdStack.classList.add(\"is-xfade\");\n\n  const layout = () => {\n    // One lead-in segment (step 1 comes up) + a hold on each step + a transition\n    // between steps. Each segment is ~0.85 screen of scroll.\n    wwdStack.style.height = window.innerHeight * (2 * N) * 0.85 + \"px\";\n  };\n\n  // Scroll progress (0..1) -> step position with a lead-in entrance for step 0\n  // (pos animates -1 -> 0 so it comes up), a hold on each step, and an eased\n  // transition between consecutive steps.\n  const progressToPosition = (p) => {\n    const segs = 2 * N; // lead-in + (N holds + (N-1) transitions)\n    const unit = 1 / segs;\n    const idx = Math.min(segs - 1, Math.floor(p / unit));\n    const local = Math.max(0, Math.min(1, (p - idx * unit) / unit));\n    if (idx === 0) return -1 + smooth(local); // lead-in: step 0 comes up into view\n    const j = idx - 1; // standard hold/transition region\n    if (j % 2 === 0) return j / 2; // hold on a step\n    return (j - 1) / 2 + smooth(local); // transition to the next step\n  };\n\n  const apply = (panel, op, y, scale, z) => {\n    panel.style.setProperty(\"--p-op\", op.toFixed(3));\n    panel.style.setProperty(\"--p-y\", y.toFixed(1) + \"px\");\n    panel.style.setProperty(\"--p-scale\", scale.toFixed(3));\n    panel.style.zIndex = String(z);\n  };\n\n  const update = () => {\n    ticking = false;\n    const vh = window.innerHeight;\n    const distance = wwdStack.offsetHeight - vh;\n    if (distance <= 0) {\n      wwdPanels.forEach((panel, i) =>\n        apply(panel, i === 0 ? 1 : 0, 0, 1, i === 0 ? 1 : 0)\n      );\n      return;\n    }\n    const p = Math.min(\n      1,\n      Math.max(0, -wwdStack.getBoundingClientRect().top / distance)\n    );\n    const pos = progressToPosition(p);\n    wwdPanels.forEach((panel, i) => {\n      const d = i - pos; // <0 leaving (fading out), >0 entering (coming up)\n      const lin = Math.max(0, 1 - Math.abs(d) / FADE_SPAN);\n      const op = smooth(lin);\n      let y, scale;\n      if (d >= 0) {\n        // ENTERING — the next step comes up from below into place as it fades in.\n        y = d * RISE_IN;\n        scale = 1;\n      } else {\n        // LEAVING — current step fades out, drifting up a touch as the next comes up.\n        y = d * RISE_OUT;\n        scale = 0.965 + 0.035 * op;\n      }\n      apply(panel, op, y, scale, Math.round(op * 100));\n    });\n  };\n\n  const onScroll = () => {\n    if (!ticking) {\n      ticking = true;\n      requestAnimationFrame(update);\n    }\n  };\n\n  layout();\n  update();\n  window.addEventListener(\"scroll\", onScroll, { passive: true });\n  window.addEventListener(\"resize\", () => {\n    layout();\n    update();\n  });\n}\n\n// Mobile (≤760px): scroll-reveal entrance animations. Desktop has its own\n// scroll-driven choreography, so this only runs on phones. Elements fade/slide\n// up as they enter the viewport.\nconst wwdM = document.querySelector(\".wwd-stack\");\nconst wwdMPanels = wwdM ? Array.from(wwdM.querySelectorAll(\".wwd-panel\")) : [];\nif (\n  wwdM &&\n  wwdMPanels.length > 1 &&\n  window.matchMedia(\"(max-width: 760px)\").matches &&\n  !(window.matchMedia && window.matchMedia(\"(prefers-reduced-motion: reduce)\").matches)\n) {\n  const N = wwdMPanels.length;\n  const TRACK_VH = 1.15 * N;\n  const SLIDE = 54;\n  const FADE_SPAN = 0.62;\n  const smooth = (t) => t * t * (3 - 2 * t);\n  let ticking = false;\n  const pin = wwdM.querySelector(\".wwd-stack-pin\") || wwdM;\n  const dotsWrap = document.createElement(\"div\");\n  dotsWrap.className = \"wwd-mscroll-dots\";\n  dotsWrap.setAttribute(\"aria-hidden\", \"true\");\n  for (let i = 0; i < N; i++) dotsWrap.appendChild(document.createElement(\"span\"));\n  pin.appendChild(dotsWrap);\n  const dots = Array.from(dotsWrap.children);\n  const layout = () => { wwdM.style.height = window.innerHeight * TRACK_VH + \"px\"; };\n  const progressToPosition = (p) => {\n    const segs = N + (N - 1);\n    const unit = 1 / segs;\n    const idx = Math.min(segs - 1, Math.floor(p / unit));\n    const local = (p - idx * unit) / unit;\n    if (idx % 2 === 0) return idx / 2;\n    return (idx - 1) / 2 + smooth(local);\n  };\n  const update = () => {\n    ticking = false;\n    const vh = window.innerHeight;\n    const distance = wwdM.offsetHeight - vh;\n    if (distance <= 0) return;\n    const p = Math.min(1, Math.max(0, -wwdM.getBoundingClientRect().top / distance));\n    const pos = progressToPosition(p);\n    const active = Math.round(pos);\n    wwdMPanels.forEach((panel, i) => {\n      const d = i - pos;\n      const op = Math.max(0, 1 - Math.abs(d) / FADE_SPAN);\n      panel.style.opacity = op.toFixed(3);\n      panel.style.transform = `translate3d(0, ${(d * SLIDE).toFixed(1)}px, 0)`;\n    });\n    dots.forEach((dot, i) => dot.classList.toggle(\"is-active\", i === active));\n  };\n  const onScroll = () => { if (!ticking) { ticking = true; requestAnimationFrame(update); } };\n  wwdM.classList.add(\"wwd-mscroll\");\n  layout();\n  update();\n  window.addEventListener(\"scroll\", onScroll, { passive: true });\n  window.addEventListener(\"resize\", () => { layout(); update(); });\n}\n\nif (window.matchMedia(\"(max-width: 760px)\").matches && \"IntersectionObserver\" in window) {\n  const revealSelectors = [\n    \".home-hero-copy > *\",\n    \".home-calendar-link\",\n    \".home-intro-copy p\",\n    \".intro-button\",\n    \".home-intro-farm\",\n    \".home-categories-heading\",\n    \".home-products-kicker\",\n    \".home-products-section h2\",\n    \".home-products-cta\",\n    \".product-image-card\",\n    \".bc-logo\",\n    \".home-grow-ov .wGrow\",\n    \".home-grow-ov .wTeam\",\n    \".home-grow-ov .wProcessPill\",\n    \".home-ftr-ov\",\n  ].join(\",\");\n\n  const revealEls = Array.from(document.querySelectorAll(revealSelectors));\n  revealEls.forEach((el, i) => {\n    el.classList.add(\"m-reveal\");\n    // small stagger for grids/lists so items don't pop in all at once\n    el.style.transitionDelay = (i % 4) * 70 + \"ms\";\n  });\n\n  const mReveal = new IntersectionObserver(\n    (entries) => {\n      entries.forEach((entry) => {\n        if (entry.isIntersecting) {\n          entry.target.classList.add(\"m-in\");\n          mReveal.unobserve(entry.target);\n        }\n      });\n    },\n    { threshold: 0.12, rootMargin: \"0px 0px -6% 0px\" }\n  );\n  revealEls.forEach((el) => mReveal.observe(el));\n}\n";
export default legacyScript;
